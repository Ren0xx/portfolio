import { Typography } from "@mui/material";
const About = () => {
    return (
        <section id='About'>
            <Typography variant='h2'>About</Typography>
        </section>
    );
};

export default About;
